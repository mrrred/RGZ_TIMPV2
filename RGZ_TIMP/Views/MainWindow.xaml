<Window x:Class="RGZ_TIMP.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:RGZ_TIMP.ViewModels"
        xmlns:controls="clr-namespace:RGZ_TIMP.Views.Controls"
        Title="Графопостроитель" Height="700" Width="1100">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Меню -->
        <Menu Grid.Row="0">
            <MenuItem Header="Команды">
                <MenuItem Header="Создать" Command="{Binding NewProjectCommand}"/>
                <MenuItem Header="Открыть" Command="{Binding LoadCommand}"/>
                <MenuItem Header="Сохранить" Command="{Binding SaveCommand}"/>
            </MenuItem>
            <MenuItem Header="Анимация" Command="{Binding AnimationSettingsCommand}" IsEnabled="{Binding HasProject}"/>
            <MenuItem Header="Выполнить" Command="{Binding RunAnimationCommand}" IsEnabled="{Binding HasProject}"/>
            <MenuItem Header="Остановить" Command="{Binding StopAnimationCommand}" IsEnabled="{Binding HasProject}"/>
            <MenuItem Header="?" Command="{Binding ShowHelpCommand}"/>
        </Menu>

        <!-- Canvas с узлами и рёбрами -->
        <Canvas Grid.Row="1" Background="WhiteSmoke"
                IsEnabled="{Binding HasProject}"
                MouseRightButtonDown="Canvas_MouseRightButtonDown"
                MouseMove="Canvas_MouseMove"
                MouseLeftButtonUp="Canvas_MouseLeftButtonUp">
            <Canvas.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Добавить" Command="{Binding AddNodeCommand}"/>
                </ContextMenu>
            </Canvas.ContextMenu>

            <!-- Preview line -->
            <Line x:Name="PreviewLine"
                  X1="{Binding PreviewX1}" Y1="{Binding PreviewY1}"
                  X2="{Binding PreviewX2}" Y2="{Binding PreviewY2}"
                  Stroke="#4A3D3D3D" StrokeThickness="2" StrokeDashArray="3,2">
                <Line.Style>
                    <Style TargetType="Line">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsConnecting}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Line.Style>
            </Line>

            <!-- Рёбра -->
            <ItemsControl ItemsSource="{Binding Edges}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <controls:GraphEdgeView/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Узлы -->
            <ItemsControl ItemsSource="{Binding Nodes}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Left" Value="{Binding X}"/>
                        <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <controls:GraphNodeView/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Canvas>

        <!-- Строка состояния -->
        <Border Grid.Row="2" Padding="10" Background="#F1F1F1">
            <TextBlock Text="{Binding ModeText}" FontWeight="SemiBold"/>
        </Border>
    </Grid>
</Window>