<UserControl x:Class="RGZ_TIMP.Views.Controls.GraphNodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="80" Height="80" ClipToBounds="False">
    <UserControl.ContextMenu>
        <ContextMenu>
            <MenuItem Header="Удалить" Command="{Binding DeleteCommand}"/>
        </ContextMenu>
    </UserControl.ContextMenu>
    <UserControl.ToolTip>
        <StackPanel>
            <TextBlock Text="{Binding Number, StringFormat='Узел #{0}'}"/>
            <TextBlock Text="{Binding NodeCode}"/>
        </StackPanel>
    </UserControl.ToolTip>

    <Grid x:Name="LayoutRoot"
          MouseLeftButtonDown="OnMouseLeftButtonDown"
          MouseMove="OnMouseMove"
          MouseLeftButtonUp="OnMouseLeftButtonUp">
        <!-- Основной круг -->
        <Ellipse Stroke="Black" StrokeThickness="2" Width="60" Height="60" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Ellipse.Style>
                <Style TargetType="Ellipse">
                    <Setter Property="Fill" Value="White"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsHighlighted}" Value="True">
                            <Setter Property="Fill" Value="Gold"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsStartHighlighted}" Value="True">
                            <Setter Property="Fill" Value="IndianRed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Ellipse.Style>
        </Ellipse>
        <TextBlock Text="{Binding Number}" FontSize="16" FontWeight="SemiBold"
                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <!-- Handle для создания дуг (Ctrl) -->
        <Ellipse x:Name="Handle" Width="10" Height="10" Fill="Black"
                 HorizontalAlignment="Left" VerticalAlignment="Top"
                 Visibility="Collapsed" Cursor="Hand"
                 MouseLeftButtonDown="Handle_MouseLeftButtonDown"
                 MouseLeftButtonUp="Handle_MouseLeftButtonUp"/>
    </Grid>
</UserControl>